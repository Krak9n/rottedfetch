struct App {
    username: String,
    os: String,
    kernel: String,
    cpu: String,
    gpu: String,
    memory: u32,
    de: String,
}

impl App {
    pub fn new() -> Self {
        Self {
            
        }
    }

    fn calculate_box() -> u32 {
        0
    }
}

fn main() {
    let fetch = App::new();
}
